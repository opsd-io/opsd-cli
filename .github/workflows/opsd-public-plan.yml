name: Public infrastructure plan

on:
  pull_request:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  plan:
    uses: opsd-io/public-workflows/.github/workflows/opsd-public-plan.yml@main
    with:
      compatibility-file: ci/public-compatibility.yaml
