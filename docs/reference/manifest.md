# Manifest Contract

The active manifest contract lives in [`../manifest.md`](../manifest.md).

This page exists as a reference landing page for the public docs tree. The contract itself lives in
the root-level manifest specification.
